# Backend API Configuration
# Replace with your actual backend URL
VITE_API_BASE_URL=http://localhost:3000/api/v1

# Supabase Configuration
# Get these from https://supabase.com/dashboard → Settings → API
# 1. Go to https://supabase.com/dashboard
# 2. Select your project "lufhgsgubvxjrrcsevte"
# 3. Click "Settings" in the sidebar
# 4. Go to "API" tab
# 5. Copy "Project URL" → VITE_SUPABASE_URL
# 6. Copy "Anon Public Key" → VITE_SUPABASE_ANON_KEY

VITE_SUPABASE_URL=https://lufhgsgubvxjrrcsevte.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_RzAA52ZFGw9LV2piNWd4bA_3wVwdbDP

# Environment
# Set to "development", "staging", or "production"
VITE_ENV=development
VITE_ENABLE_REALTIME=false

# Debug Flags (set to 'true' to enable verbose logging)
# VITE_DEBUG_AUTH=true    # Log authentication events
# VITE_DEBUG_API=true     # Log all API requests and responses

# Notes:
# - Never commit this file with real credentials
# - Copy this to .env or .env.local and fill in your values
# - The backend API (localhost:3000) handles Supabase auth
# - Frontend uses JWT tokens from Supabase for API requests
# - JWT tokens are automatically attached to all API calls
